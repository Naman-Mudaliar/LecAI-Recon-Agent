__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.env
.claude/

# old attempts, kept locally for reference, never committed
_archive_v1/

# downloaded/derived gtfs data - regenerable from the two real sources,
# dont belong in git (and the raw region file is huge anyway)
data/nw_gtfs.zip
data/nw_gtfs/
data/*.json
